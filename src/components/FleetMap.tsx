export default function FleetMap({ loads }: { loads?: any[] }) {
  return <div style={{ background: "#F9FAFB", borderRadius: 16, height: 200, display: "flex", alignItems: "center", justifyContent: "center", color: "#6B7280", fontSize: 14 }}>🗺️ Fleet Map</div>;
}
