export default function StopsBuilder({ stops, onChange }: { stops?: any[]; onChange?: (s: any[]) => void }) {
  return <div style={{ padding: 12, border: "1px solid #E5E7EB", borderRadius: 12, color: "#6B7280", fontSize: 13 }}>+ Add stops</div>;
}
